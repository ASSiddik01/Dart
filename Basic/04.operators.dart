void main() {
  // operator process the operands
  // arithmatic operator : +, -, *, /, %, ++, --
  // relational operator : >, <, >=, <=, ==, !=
  // assignment operator : ??= += -=, *=, /=
  // logical operator : && || !
}
